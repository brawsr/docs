title = "brawsr developer docs"

[extend]
useDefault = true

[allowlist]
description = "Locked SHA-256 values are public artifact identities, not credentials"
regexTarget = "line"
regexes = [
  '''"[a-z0-9_]*sha256":\s*"[0-9a-f]{64}"''',
]
