# Security

Use GitHub private vulnerability reporting for security issues. Do not open a
public issue with vulnerability details.

Never include API keys, authenticated CDP URLs, cookies, checkpoint contents,
or customer data in a report, screenshot, or log. The documentation site is a
static export and must not collect credentials or proxy authenticated API
requests.
